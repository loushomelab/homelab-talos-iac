apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: traefik
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    link.argocd.argoproj.io/external-link: https://traefik.646499453.xyz:8443
spec:
  project: infra
  sources:
    - repoURL: 'https://helm.traefik.io/traefik'
      chart: traefik
      targetRevision: 41.1.1
      helm:
        valueFiles:
          - $values/argocd/configs/infrastructure/traefik/values.yaml
    - repoURL: 'https://github.com/loushomelab/homelab-talos-iac.git'
      targetRevision: HEAD
      ref: values
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: traefik
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
