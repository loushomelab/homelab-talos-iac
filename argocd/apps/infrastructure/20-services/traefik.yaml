apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: traefik
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  annotations:
    link.argocd.argoproj.io/external-link: https://traefik.646499453.xyz:8443
spec:
  project: infra
  sources:
  - repoURL: 'ghcr.io/traefik/helm'
    chart: traefik
    targetRevision: 41.2.0
    helm:
      valuesObject:
        env:
          - name: TZ
            value: Asia/Shanghai
        deployment:
  # -- Enable deployment
          enabled: true
  # -- Deployment or DaemonSet
          kind: DaemonSet


# @schema mergeProperties: true
        ports:
          web:
            http:
              redirections:
                entryPoint:
                  to: websecure
                  scheme: https
          websecure:
            port: 8443
            exposedPort: 8443
            http:
              tls:
                enabled: true

# 强制 Traefik 使用 MetalLB 池子里的第一个 IP，方便你在 Surge 里做 DNS 映射
        service:
          type: LoadBalancer
          spec:
            loadBalancerIP: 192.168.50.230

        providers:
          kubernetesCRD:
            enabled: true
            allowCrossNamespace: true
            allowExternalNameServices: true
          kubernetesIngress:
            enabled: true
            allowExternalNameServices: true
        ingressRoute:
          dashboard:
            enabled: true
            annotations:
              gethomepage.dev/enabled: "true"
              gethomepage.dev/name: "Traefik"
              gethomepage.dev/description: "Ingress Controller"
              gethomepage.dev/group: "Infrastructure"
              gethomepage.dev/icon: "sh-traefik.svg"
              gethomepage.dev/href: "https://traefik.646499453.xyz:8443"
              gethomepage.dev/pod-selector: "app.kubernetes.io/name=traefik"
              # Traefik service widget: pulls routers/services/middleware from
              # the Traefik API on port 8080 (cluster-internal). Chart must keep
              # the traefik Service exposing 8080 for this to be reachable.
              gethomepage.dev/widget.type: "traefik"
              gethomepage.dev/widget.url: "http://traefik.traefik.svc.cluster.local:8080"
            matchRule: Host(`traefik.646499453.xyz`)
            entryPoints: ["websecure"]
            middlewares:
            - name: authentik-forwardauth
            tls:
              secretName: wildcards-646499453-xyz-tls

        extraObjects:
        - apiVersion: traefik.io/v1alpha1
          kind: Middleware
          metadata:
            name: authentik-forwardauth
            namespace: traefik
          spec:
            forwardAuth:
              address: "http://authentik-server.authentik.svc.cluster.local:80/outpost.goauthentik.io/auth/traefik"
              trustForwardHeader: true
              maxResponseBodySize: 2097152
              authResponseHeaders:
              - X-authentik-username
              - X-authentik-groups
              - X-authentik-email
              - X-authentik-name
              - X-authentik-uid
              - X-authentik-jwt
              - X-authentik-meta-jwks
              - X-authentik-meta-outpost
              - X-authentik-meta-provider
              - X-authentik-meta-app
              - X-authentik-meta-version
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: traefik
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
